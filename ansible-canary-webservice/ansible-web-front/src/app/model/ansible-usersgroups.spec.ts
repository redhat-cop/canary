import { AnsibleUsersgroups } from './ansible-usersgroups';

describe('AnsibleUsersgroups', () => {
  it('should create an instance', () => {
    expect(new AnsibleUsersgroups()).toBeTruthy();
  });
});
